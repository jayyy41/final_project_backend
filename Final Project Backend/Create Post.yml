info:
  name: Create Post
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/posts
  body:
    type: json
    data: |-
      {
        "title": "My first post",
        "body": "Hello!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
