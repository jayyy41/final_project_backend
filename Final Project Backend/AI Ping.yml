info:
  name: AI Ping
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3000/api/ai/ping
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
